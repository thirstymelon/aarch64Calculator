{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ARM64 Assembly",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${fileDirname}",
            "preLaunchTask": "Build and Run ARM64",
            "terminal": "integrated"
        }
    ]
}
